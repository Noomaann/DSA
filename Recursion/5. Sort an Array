void inserts(vector<int>&v, int temp)
{
	// base case
	if (v.size() == 0 || v[v.size() - 1] <= temp)
	{
		v.push_back(temp);
		return;
	}
	int val = v[v.size() - 1]; // induction
	v.pop_back(); // induction
	inserts(v, temp); // hypothesis 
	v.push_back(val); // induction
}

void sort(vector<int>&v)
{
	if (v.size() == 1) return; // base case

	int temp = v[v.size() - 1]; // induction
	v.pop_back(); // induction
	sort(v); // hypothesis
	inserts(v, temp); // induction
}

void solve() {
	int n;
	cin >> n;
	vi v(n);
	forin(v, n)

	sort(v);

	for (auto it : v) cout << it << " ";
}
